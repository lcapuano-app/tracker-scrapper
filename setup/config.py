# PROD will start a fake display for selenium. DEV won't
env = 'PROD'

# Type of tracker.gg valorants stats playlist
#(profile, map, agent, weapon or match)
type = 'PROFILE'

# Riot id username#tag. I'm using henrik as default because I do not expect him to change it
riot_id =  'Henrik3#EUW3'

# The whole config
config = {
  'env': env,
  'type': type,
  'riot_id': riot_id
}
